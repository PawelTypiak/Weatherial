<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/colorPrimary"
    android:id="@+id/first_launch_loading_fragment_layout">
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:id="@+id/first_launch_loading_fragment_content_layout"
        >
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:textColor="@color/textPrimaryDarkBackground"
            android:textSize="@dimen/first_launch_configuration_text_size"
            android:visibility="invisible"
            android:id="@+id/first_launch_loading_fragment_message"
            />
        <ProgressBar
            android:indeterminate="true"
            style="?android:attr/progressBarStyle"
            android:layout_height="64dp"
            android:layout_width="64dp"
            android:visibility="invisible"
            android:layout_gravity="center"
            android:layout_marginTop="20dp"
            android:theme="@style/AppTheme.White"
            android:id="@+id/first_launch_loading_fragment_progress_circle"
            />
        <View
            android:layout_width="match_parent"
            android:layout_height="40dp"
            android:visibility="gone"
            android:id="@+id/first_launch_loading_fragment_margin_view"
            />
    </LinearLayout>
</RelativeLayout>
